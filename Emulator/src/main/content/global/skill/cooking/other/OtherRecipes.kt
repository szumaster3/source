package content.global.skill.cooking.other

/*
    // Admiral Pie Recipe
    onUseWith(IntType.ITEM, Items.PIE_SHELL_2315, *arrayOf(Items.SALMON_329, Items.TUNA_361, Items.POTATO_1942)) { player, used, with ->
        if (removeItem(player, used.asItem()) && removeItem(player, with.asItem())) {
            addItem(player, Items.RAW_ADMIRAL_PIE_7196)
            sendMessage(player, "You prepare an admiral pie.")
        }
        return@onUseWith true
    }

    // Fish Pie Recipe
    onUseWith(IntType.ITEM, Items.PIE_SHELL_2315, *arrayOf(Items.TROUT_333, Items.COD_339, Items.POTATO_1942)) { player, used, with ->
        if (removeItem(player, used.asItem()) && removeItem(player, with.asItem())) {
            addItem(player, Items.RAW_FISH_PIE_7186)
            sendMessage(player, "You prepare a fish pie.")
        }
        return@onUseWith true
    }

    // Garden Pie Recipe
    onUseWith(IntType.ITEM, Items.PIE_SHELL_2315, *arrayOf(Items.TOMATO_1982, Items.ONION_1957, Items.CABBAGE_1965)) { player, used, with ->
        if (removeItem(player, used.asItem()) && removeItem(player, with.asItem())) {
            addItem(player, Items.RAW_GARDEN_PIE_7176)
            sendMessage(player, "You prepare a garden pie.")
        }
        return@onUseWith true
    }

    // Meat Pie Recipe
    onUseWith(IntType.ITEM, Items.PIE_SHELL_2315, *arrayOf(Items.COOKED_MEAT_2142, Items.COOKED_CHICKEN_2140, Items.COOKED_RABBIT_3228)) { player, used, with ->
        if (removeItem(player, used.asItem()) && removeItem(player, with.asItem())) {
            addItem(player, Items.UNCOOKED_MEAT_PIE_2319)
            sendMessage(player, "You fill the pie with meat.")
        }
        return@onUseWith true
    }

    // Mud Pie Recipe
    onUseWith(IntType.ITEM, Items.PIE_SHELL_2315, *arrayOf(Items.COMPOST_6032, Items.BUCKET_OF_WATER_1929, Items.CLAY_434)) { player, used, with ->
        if (removeItem(player, used.asItem()) && removeItem(player, with.asItem())) {
            addItem(player, Items.RAW_MUD_PIE_7168)
            sendMessage(player, "You prepare a mud pie.")
        }
        return@onUseWith true
    }

    // Summer Pie Recipe
    onUseWith(IntType.ITEM, Items.PIE_SHELL_2315, *arrayOf(Items.STRAWBERRY_5504, Items.WATERMELON_5982, Items.COOKING_APPLE_1955)) { player, used, with ->
        if (removeItem(player, used.asItem()) && removeItem(player, with.asItem())) {
            addItem(player, Items.RAW_SUMMER_PIE_7216)
            sendMessage(player, "You prepare a summer pie.")
        }
        return@onUseWith true
    }

    // Wild Pie Recipe
    onUseWith(IntType.ITEM, Items.PIE_SHELL_2315, *arrayOf(Items.RAW_BEAR_MEAT_2136, Items.RAW_CHOMPY_2876, Items.RAW_RABBIT_3226)) { player, used, with ->
        if (removeItem(player, used.asItem()) && removeItem(player, with.asItem())) {
            addItem(player, Items.RAW_WILD_PIE_7206)
            sendMessage(player, "You prepare a wild pie.")
        }
        return@onUseWith true
    }

    // Spicy Sauce Recipe
    onUseWith(IntType.ITEM, Items.BOWL_1923, *arrayOf(Items.GARLIC_1550, Items.GNOME_SPICE_2169)) { player, used, with ->
        if (removeItem(player, used.asItem()) && removeItem(player, with.asItem())) {
            addItem(player, Items.SPICY_SAUCE_7072)
            sendMessage(player, "You prepare the spicy sauce.")
        }
        return@onUseWith true
    }
}


 */