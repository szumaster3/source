package core.game.world.map

/**
 * An enum holding commonly used distances (in tiles of the map).
 *
 * @author Emperor
 */
enum class MapDistance(val distance: Int) {
    RENDERING(15),
    SOUND(5),
}