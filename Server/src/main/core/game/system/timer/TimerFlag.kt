package core.game.system.timer

/**
 * Flags used to define special behaviors for timers.
 */
enum class TimerFlag {
    /**
     * Timer will be cleared automatically when the entity dies.
     */
    ClearOnDeath,
}